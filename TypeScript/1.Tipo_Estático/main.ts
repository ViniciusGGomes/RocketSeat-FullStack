/*
  TypeScript -> verifica o erro em tempo de compilação

  JavaScript -> detecta erros apenas em tempo de execução

*/

const message = "hello typescript"

// message = "oi" //assign -> atribuir

// message()

const users= {
  name: "Vinícius",
  city: "Palmas"
}

// console.log(users.outro)